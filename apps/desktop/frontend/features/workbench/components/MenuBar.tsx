// Native macOS menu is now built in Rust (see src‑tauri/src/menu.rs).
// On macOS this component is rendered as empty; on other platforms
// a custom React menu could be added later.
export function MenuBar() {
  return null;
}
