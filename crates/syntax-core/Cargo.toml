[package]
name = "syntax-core"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
tree-sitter = "0.22"
tree-sitter-rust = { version = "0.24.2", optional = true }
ropey = "1.6"
thiserror = "2"
parking_lot = "0.12"

[features]
default = ["rust"]
rust = ["tree-sitter-rust"]
