[package]
name = "syntax-core"
version = "0.1.0"
edition = "2024"

[dependencies]
tree-sitter = { version = "0.22", features = ["highlight"] }
tree-sitter-rust = "0.24"
tree-sitter-toml = "0.22"
ropey = "1.6"
thiserror = "1.0"

[features]
default = []
