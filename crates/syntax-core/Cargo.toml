[package]
name = "syntax-core"
version = "0.1.0"
edition = "2024"

[dependencies]
tree-sitter = "0.22"
tree-sitter-rust = { version = "0.24", optional = true }
ropey = "1.6"
thiserror = "1.0"

[features]
default = ["rust"]
rust = ["tree-sitter-rust"]
